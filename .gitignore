# Firmware build artifacts (baixados do CI ou gerados localmente)
*.bin
*.uf2
*.hex
*.elf

# Assets binários embedados no firmware (sprites, fontes) — exceção ao *.bin
!dongle/src/assets/**/*.bin

# Ferramentas externas baixadas localmente (não versionar binários)
tools/*.exe

# Cargo / Rust (F2+)
target/
**/target/

# Ferramentas de sistema
.DS_Store
Thumbs.db
*.swp
*~

# IDE
.idea/
.vscode/launch.json
.vscode/tasks.json
